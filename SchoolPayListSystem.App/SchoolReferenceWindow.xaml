<Window x:Class="SchoolPayListSystem.App.SchoolReferenceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="School Reference - Help" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="White"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize">
    <Grid>
        <DockPanel>
            <!-- Header -->
            <Grid DockPanel.Dock="Top" Height="60" Background="#003D7A">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,0,0">
                    <TextBlock Text="School Reference Guide" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- Instructions -->
            <TextBlock DockPanel.Dock="Top" Text="Below is the list of all available schools. Use this as a reference when entering school codes in the payment list." 
                       Foreground="#666666" FontSize="11" TextWrapping="Wrap" Margin="15,10,15,5"/>

            <!-- Search Section -->
            <Grid DockPanel.Dock="Top" Height="45" Margin="15,5,15,10" Background="#F5F5F5" VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Search by School Code:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
                <TextBox x:Name="SchoolCodeSearchBox" Grid.Column="1" Padding="8" Height="35" VerticalContentAlignment="Center" TextChanged="SchoolCodeSearch_TextChanged"/>
                <TextBlock Grid.Column="2" Text="Search by School Name:" VerticalAlignment="Center" Margin="15,0,10,0" FontWeight="Bold"/>
                <TextBox x:Name="SchoolNameSearchBox" Grid.Column="3" Padding="8" Height="35" VerticalContentAlignment="Center" TextChanged="SchoolNameSearch_TextChanged"/>
                <Button Grid.Column="4" Content="Clear Search" Height="35" Width="100" Margin="15,0,0,0" Background="#FF9900" Foreground="White" FontWeight="Bold" Click="ClearSearch_Click" Cursor="Hand"/>
            </Grid>

            <!-- Data Grid -->
            <DataGrid x:Name="SchoolsDataGrid" DockPanel.Dock="Top" AutoGenerateColumns="False" 
                      Margin="15,10,15,10" Height="400"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      CanUserSortColumns="False"
                      CanUserReorderColumns="False"
                      GridLinesVisibility="All"
                      BorderThickness="1"
                      BorderBrush="#CCCCCC"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="School Code" Binding="{Binding SchoolCode}" Width="120" IsReadOnly="True"/>
                    <DataGridTextColumn Header="School Name" Binding="{Binding SchoolName}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="School Type" Binding="{Binding SchoolType.TypeName}" Width="150" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Branch Name" Binding="{Binding Branch.BranchName}" Width="150" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Close Button -->
            <Grid DockPanel.Dock="Bottom" Height="50" Margin="15,10,15,15">
                <Button Content="CLOSE" Height="40" Background="#0066CC" Foreground="White" FontSize="12" FontWeight="Bold" Click="Close_Click" Cursor="Hand" BorderThickness="0"/>
            </Grid>
        </DockPanel>
    </Grid>
</Window>
